# local env / secrets
.env
.env.*
!.env.example
# direnv file (the ensure-envrc.sh SessionStart hook symlinks it into worktrees)
.envrc

# runtime
*.log
node_modules/
dist/

# macOS
.DS_Store

# Per-ticket build plans are local working artifacts (carry Linear URLs/ticket detail);
# they stay out of the public repo. The meta extraction plan docs/plan.md is tracked.
docs/plans/
