# local env / secrets
.env
.env.*
!.env.example
# direnv file (the ensure-envrc.sh SessionStart hook symlinks it into worktrees)
.envrc

# runtime
*.log
node_modules/
dist/

# macOS
.DS_Store

# Local per-machine ticket-flow binding (Linear UUIDs/identity — never commit)
.claude/ticket-flow.json
