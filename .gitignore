# local env / secrets
.env
.env.*
!.env.example
# direnv file (the ensure-envrc.sh SessionStart hook symlinks it into worktrees)
.envrc

# runtime
*.log
node_modules/
dist/

# Claude Code local worktree/config state
.claude/worktrees/
.claude/ticket-flow.json

# macOS
.DS_Store
